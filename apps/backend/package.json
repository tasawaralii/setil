{
  "name": "setil-backend",
  "version": "0.1.0",
  "description": "Setil Backend API",
  "scripts": {
    "dev": "uv run python main.py",
    "start": "uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
