# Setil Backend

Backend for Setil application, managed with `uv`.

## Setup

1. Install `uv` if you haven't already.
2. Run `uv sync` to install dependencies.

## Development

Run the development server:

```bash
uv run uvicorn app.main:app --reload
```
